# CodeTrail

Платформа для изучения программирования: каталог курсов, последовательная разблокировка уроков, серверная проверка тестов, прогресс, опыт (XP) и серия активных дней (streak).

> Проект в разработке. Раздел «Запуск» будет добавлен по мере готовности бэкенда и фронтенда.

## Стек

**Бэкенд**
- ASP.NET Core 8 Web API
- Entity Framework Core (Code First, миграции коммитятся в репозиторий)
- PostgreSQL или SQLite
- JWT-аутентификация
- FluentValidation / DataAnnotations
- Serilog / встроенный ILogger
- xUnit (модульные тесты)
- Swagger / Swashbuckle

**Фронтенд**
- React 18 + TypeScript (Vite) *(выбор между React и Blazor WebAssembly будет зафиксирован позже)*

## Архитектура

Решение разделено на слои с однонаправленными зависимостями:

```
Api  →  Application  →  Domain
 └──────→  Infrastructure  ↗
```

| Слой | Назначение |
|---|---|
| **CodeTrail.Domain** | Сущности предметной области, без зависимостей от других слоёв |
| **CodeTrail.Application** | Бизнес-логика, сервисы, интерфейсы, DTO; зависит только от Domain |
| **CodeTrail.Infrastructure** | EF Core DbContext, доступ к данным, реализации интерфейсов Application; зависит от Domain и Application |
| **CodeTrail.Api** | Контроллеры, конфигурация DI, точка входа приложения; зависит от Application и Infrastructure |

Контроллеры не содержат бизнес-логики и не обращаются к DbContext напрямую. Наружу отдаются только DTO — сущности EF за пределы Infrastructure/Application не выходят.

## Структура репозитория

```
CodeTrail.sln
global.json                     # фиксирует SDK .NET 8
src/
  CodeTrail.Api/
  CodeTrail.Application/
  CodeTrail.Domain/
  CodeTrail.Infrastructure/
tests/
  CodeTrail.Tests/
```

## Роли

| Роль | Возможности |
|---|---|
| Гость | Каталог курсов, страница курса, регистрация/вход |
| Студент | + запись на курс, прохождение уроков и тестов, прогресс, рейтинг |
| Автор/Администратор | + CRUD курсов, уроков, вопросов, статистика по студентам |

## Статус разработки

- [x] Структура решения, слои, project references, git-репозиторий
- [ ] Сущности, DbContext, миграции, сидирование контента
- [ ] Аутентификация и роли
- [ ] Каталог курсов, запись на курс
- [ ] Выдача урока, последовательная разблокировка
- [ ] Проверка ответов, прогресс, опыт, серия дней
- [ ] Тесты, обработка ошибок, логирование, Swagger
- [ ] Фронтенд
